# ThreatWeave — dependencies
# All free / open-source. No paid tiers required.

google-generativeai>=0.5.0    # Gemini free tier: 15 req/min, 1M tokens/day
networkx>=3.2.1                # Attack-chain prediction graph
pyyaml>=6.0.1                  # MITRE signature rules
reportlab>=4.1.0               # PDF report generation
jinja2>=3.1.3                  # HTML report generation
pillow>=10.2.0                 # Image support in PDF
python-dateutil>=2.9.0         # Timestamp parsing
requests>=2.31.0               # MITRE CTI download script
tqdm>=4.66.0                   # Progress bars
pytest>=8.1.0                  # Testing

# Optional — only needed for binary .evtx files
# python-evtx>=0.7.4
